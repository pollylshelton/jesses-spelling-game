<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jesse's Galactic Academy</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Outfit:wght@300;400;600&display=swap" rel="stylesheet">
    
    <!-- Firebase Integration -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInAnonymously, signInWithCustomToken, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
        import { getFirestore, doc, setDoc, getDoc, onSnapshot } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

        // Firebase Configuration (Provided by environment)
        const firebaseConfig = JSON.parse(__firebase_config);
        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);
        const db = getFirestore(app);
        const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-app-id';

        // Global Firebase state
        window.galaxyDB = {
            db, auth, appId,
            user: null,
            isInitialized: false
        };

        // Authentication Handshake (Rule 3)
        const initAuth = async () => {
            try {
                if (typeof __initial_auth_token !== 'undefined' && __initial_auth_token) {
                    await signInWithCustomToken(auth, __initial_auth_token);
                } else {
                    await signInAnonymously(auth);
                }
            } catch (err) {
                console.error("Academy Link Failure:", err);
            }
        };

        onAuthStateChanged(auth, (user) => {
            window.galaxyDB.user = user;
            window.galaxyDB.isInitialized = true;
            // Hide loading overlay once pilot is authenticated
            const overlay = document.getElementById('connection-overlay');
            if (overlay) overlay.classList.add('hidden');
        });

        initAuth();

        // Database Path Helper (Rule 1)
        window.getPilotDoc = (pilotName) => {
            // Using Rule 1 strict path for public data
            return doc(db, 'artifacts', appId, 'public', 'data', 'pilots', pilotName.toLowerCase());
        };

    </script>

    <style>
        body {
            font-family: 'Outfit', sans-serif;
            background: linear-gradient(-45deg, #030508, #0a111a, #030508, #050b14);
            background-size: 400% 400%;
            animation: livingGradient 35s ease infinite;
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center; justify-content: center;
            overflow: hidden; margin: 0;
        }

        @keyframes livingGradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        h1, h2, h3, .impact-font { font-family: 'Anton', sans-serif; text-transform: uppercase; letter-spacing: 0.1em; }

        .glass-panel {
            background: rgba(255, 255, 255, 0.01);
            backdrop-filter: blur(40px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 30px 60px rgba(0,0,0,0.5);
        }

        .profile-btn { transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
        .profile-btn:hover { transform: scale(1.15) translateY(-10px); }
        
        .helmet-frame {
            width: 220px; height: 220px; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            margin-bottom: 25px; border: 4px solid rgba(255, 255, 255, 0.05);
            background: #000; transition: all 0.4s;
            position: relative;
            box-shadow: 0 0 50px rgba(0,0,0,1);
        }
        .profile-btn:hover .helmet-frame {
            border-color: #38bdf8;
            box-shadow: 0 0 60px rgba(56, 189, 248, 0.4);
        }

        .vault-dashboard {
            background: linear-gradient(135deg, rgba(56, 189, 248, 0.08), rgba(56, 189, 248, 0.02));
            border: 1px solid rgba(56, 189, 248, 0.2);
            border-left: 4px solid #38bdf8;
        }

        .game-tile { transition: all 0.4s; background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.05); }
        .game-tile:hover { transform: translateY(-10px); border-color: rgba(255, 255, 255, 0.2); background: rgba(255, 255, 255, 0.06); }

        .word-container { display: flex; flex-wrap: wrap; justify-content: center; gap: 0.5rem; width: 100%; padding: 0 20px; }
        .blank {
            border-bottom: 4px solid rgba(56, 189, 248, 0.5);
            min-width: 2.8rem; height: 4.5rem;
            display: flex; align-items: center; justify-content: center;
            font-size: clamp(1.8rem, 6vw, 3.5rem);
            font-weight: 600;
            transition: all 0.3s;
        }

        .error-char { color: #f43f5e; animation: errorShake 0.4s ease-in-out infinite; border-bottom-color: #f43f5e !important; }
        .correct-char { color: #4ade80; border-bottom-color: #4ade80 !important; }

        @keyframes errorShake { 0%, 100% { transform: translateX(0); } 25% { transform: translateX(-3px); } 75% { transform: translateX(3px); } }

        @media print { .no-print { display: none !important; } .print-only { display: block !important; } }
        .print-only { display: none; }
        #certificate-view { background: #fff; color: #000; padding: 40px; border: 15px double #facc15; text-align: center; max-width: 800px; margin: auto; }

        .aurora {
            position: absolute; width: 80vw; height: 80vw; border-radius: 50%;
            filter: blur(120px); z-index: -1; opacity: 0.15;
            animation: drift 40s infinite alternate ease-in-out;
        }
        .aurora-1 { background: #ff0080; top: -30%; left: -20%; }
        .aurora-2 { background: #4f46e5; bottom: -20%; right: -20%; }

        @keyframes drift {
            from { transform: translate(-10%, -10%) rotate(0deg) scale(1); }
            to { transform: translate(15%, 15%) rotate(90deg) scale(1.1); }
        }

        #chatbot-panel {
            position: fixed; bottom: 20px; right: 20px; width: 340px; height: 480px;
            display: flex; flex-direction: column; z-index: 1000;
            transition: transform 0.3s ease, opacity 0.3s ease;
        }
        #chatbot-panel.hidden { transform: translateY(120%); opacity: 0; pointer-events: none; display: flex !important; }
        #chatbot-messages { flex-grow: 1; overflow-y: auto; scrollbar-width: thin; }
        .message-ai { background: rgba(56, 189, 248, 0.1); border: 1px solid rgba(56, 189, 248, 0.2); }
        .message-user { background: rgba(255, 255, 255, 0.05); align-self: flex-end; }
    </style>
</head>
<body class="p-4 sm:p-6">

    <div class="no-print">
        <div class="aurora aurora-1"></div>
        <div class="aurora aurora-2"></div>
    </div>

    <!-- CONNECTION OVERLAY -->
    <div id="connection-overlay" class="fixed inset-0 z-[2000] bg-black flex items-center justify-center transition-opacity duration-500">
        <div class="text-white impact-font text-2xl animate-pulse tracking-widest uppercase">Academy Loading...</div>
    </div>

    <!-- AI ASSISTANT -->
    <button id="chatbot-toggle" class="fixed bottom-6 right-6 w-14 h-14 rounded-full glass-panel border-sky-500/50 flex items-center justify-center z-[1100] hover:scale-110 transition no-print group">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#38bdf8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg>
    </button>

    <div id="chatbot-panel" class="hidden glass-panel rounded-[32px] border-white/10 flex flex-col no-print overflow-hidden">
        <div class="p-4 border-b border-white/10 flex justify-between items-center bg-sky-500/5">
            <span class="impact-font text-sky-400 text-xs tracking-widest uppercase font-bold">Pilot Assistant</span>
            <button onclick="toggleChat()" class="text-white/40 hover:text-white">✕</button>
        </div>
        <div id="chatbot-messages" class="p-4 space-y-3 flex flex-col text-[11px]"></div>
        <div id="typing-box" class="hidden p-3 text-[10px] text-sky-400 animate-pulse">Syncing logic...</div>
        <div class="p-3 border-t border-white/10 flex gap-2">
            <input type="text" id="chat-input" class="flex-grow bg-white/5 border border-white/10 rounded-full px-3 py-1.5 text-[11px] outline-none focus:border-sky-500 text-white" placeholder="Ask for a spelling tip...">
            <button onclick="sendMessage()" class="w-8 h-8 rounded-full bg-sky-500 flex items-center justify-center">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><polyline points="22 2 11 13"></polyline><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
            </button>
        </div>
    </div>

    <div id="app-container" class="w-full max-w-4xl relative z-10 no-print">

        <!-- SCREEN: PILOT SELECTION -->
        <div id="screen-auth" class="text-center animate-in fade-in zoom-in duration-700">
            <h1 class="text-6xl sm:text-7xl impact-font mb-16 tracking-tighter text-white/95">Who's Flying Today?</h1>
            <div class="flex flex-wrap justify-center gap-16 sm:gap-24">
                <button onclick="selectPilot('Jesse')" class="profile-btn group">
                    <div class="helmet-frame shadow-2xl overflow-hidden">
                        <svg viewBox="0 0 100 100" class="w-full h-full p-2">
                            <defs><linearGradient id="chrome-grad" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" style="stop-color:#f1f5f9"/><stop offset="50%" style="stop-color:#94a3b8"/><stop offset="100%" style="stop-color:#1e293b"/></linearGradient></defs>
                            <path d="M20 30 Q20 5 50 5 Q80 5 80 30 L80 65 Q80 85 50 85 Q20 85 20 65 Z" fill="url(#chrome-grad)" />
                            <rect x="25" y="32" width="50" height="22" rx="4" fill="#000" />
                        </svg>
                    </div>
                    <div class="text-4xl font-bold uppercase tracking-[0.4em] text-sky-400 group-hover:text-white transition-all text-center">Jesse</div>
                </button>
                <button onclick="selectPilot('Friend')" class="profile-btn group">
                    <div class="helmet-frame shadow-2xl overflow-hidden">
                        <svg viewBox="0 0 100 100" class="w-full h-full p-2">
                            <defs><linearGradient id="gold-grad" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" style="stop-color:#fbbf24"/><stop offset="50%" style="stop-color:#d97706"/><stop offset="100%" style="stop-color:#451a03"/></linearGradient></defs>
                            <path d="M20 30 Q20 5 50 5 Q80 5 80 30 L80 65 Q80 85 50 85 Q20 85 20 65 Z" fill="url(#gold-grad)" />
                            <rect x="25" y="35" width="50" height="30" rx="4" fill="#000" />
                        </svg>
                    </div>
                    <div class="text-4xl font-bold uppercase tracking-[0.3em] text-amber-500 group-hover:text-white transition-all text-center">Friend</div>
                </button>
            </div>
        </div>

        <!-- SCREEN: HOME -->
        <div id="screen-home" class="hidden space-y-10 animate-in fade-in duration-700">
            <div class="flex flex-col lg:flex-row justify-between items-start gap-10">
                <div class="text-left flex-1">
                    <h1 class="text-8xl sm:text-9xl impact-font tracking-tighter bg-clip-text text-transparent bg-gradient-to-b from-white to-white/30 leading-none mb-3 text-center sm:text-left">Academy</h1>
                    <div class="flex items-center gap-6 justify-center sm:justify-start mb-6">
                        <p class="text-[12px] tracking-[0.8em] text-sky-400 font-bold uppercase">Pilot Active: <span id="display-pilot-name" class="text-white"></span></p>
                        <button onclick="location.reload()" class="text-[10px] text-rose-400 uppercase font-bold tracking-widest hover:text-white transition underline underline-offset-4">Identity Shift</button>
                    </div>
                </div>
                
                <div class="vault-dashboard p-7 rounded-[40px] flex flex-col sm:flex-row items-center gap-10 min-w-full sm:min-w-[540px] shadow-2xl border border-white/10">
                    <div class="flex items-center gap-6">
                        <div class="p-6 bg-green-500/10 rounded-3xl text-green-400 border border-green-500/20 shadow-[0_0_20px_rgba(74,222,128,0.1)]">
                            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="5" width="20" height="14" rx="2" ry="2"></rect><line x1="2" y1="10" x2="22" y2="10"></line></svg>
                        </div>
                        <div class="text-left">
                            <div class="text-[11px] tracking-[0.3em] text-white/40 uppercase font-bold">Galactic Bank</div>
                            <div id="vault-balance" class="text-6xl impact-font text-green-400 leading-tight">0 SC</div>
                            <button onclick="showWithdraw()" class="text-[10px] text-sky-400 hover:text-white transition uppercase font-bold mt-1 block">Trade Credits</button>
                        </div>
                    </div>
                    <div class="hidden sm:block w-px h-16 bg-white/10 mx-2"></div>
                    <div class="text-left flex-1">
                        <h4 class="text-[11px] uppercase font-bold tracking-widest text-sky-400 mb-1 flex items-center gap-2"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg> Rewards Protocol</h4>
                        <p class="text-[11px] text-white/60 font-light leading-relaxed">Collect Star Credits to trade for **REAL LIFE CASH** when you print a ticket. Inquire at Bank of Mum.</p>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-8">
                <button onclick="chooseGame('lunar')" class="game-tile glass-panel p-10 rounded-[48px] text-left flex flex-col justify-between h-72 group">
                    <div class="p-5 w-fit bg-sky-500/10 rounded-2xl group-hover:bg-sky-500/30 transition-all text-sky-400"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2C12 2 12 5 12 9C12 13 11 15 8 18L7 21L12 19L17 21L16 18C13 15 12 13 12 9Z"></path><circle cx="12" cy="9" r="1"></circle></svg></div>
                    <div><h3 class="text-4xl impact-font mb-2">Lunar Mission</h3><p class="text-white/40 text-base font-light italic">Standard manual override training.</p></div>
                </button>
                <button onclick="chooseGame('echo')" class="game-tile glass-panel p-10 rounded-[48px] text-left flex flex-col justify-between h-72 group border-amber-500/20">
                    <div class="p-5 w-fit bg-amber-500/10 rounded-2xl group-hover:bg-amber-500/30 transition-all text-amber-400"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg></div>
                    <div><h3 class="text-4xl impact-font mb-2">Silent Echo</h3><p class="text-rose-400/80 uppercase font-bold text-[10px] tracking-widest">Independent Typing</p><p class="text-white/40 text-base font-light italic">Decode signals with no clues. 2 attempts.</p></div>
                </button>
                <button onclick="chooseGame('meteor')" class="game-tile glass-panel p-10 rounded-[48px] text-left flex flex-col justify-between h-72 group">
                    <div class="p-5 w-fit bg-rose-500/10 rounded-2xl group-hover:bg-rose-500/30 transition-all text-rose-400"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m13 2-2 3M20 4l-2 2M4 20l2-2M12 12l8 8"/></svg></div>
                    <div><h3 class="text-4xl impact-font mb-2">Meteor Blast</h3><p class="text-white/40 text-base font-light italic">High-velocity typing base defense.</p></div>
                </button>
                <button onclick="chooseGame('void')" class="game-tile glass-panel p-10 rounded-[48px] text-left flex flex-col justify-between h-72 group">
                    <div class="p-5 w-fit bg-indigo-500/10 rounded-2xl group-hover:bg-indigo-500/30 transition-all text-indigo-400"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><path d="M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z"></path></svg></div>
                    <div><h3 class="text-4xl impact-font mb-2">Void Choice</h3><p class="text-white/40 text-base font-light italic">Rapid fire choice under pressure.</p></div>
                </button>
            </div>
        </div>

        <!-- SETUP -->
        <div id="screen-setup" class="hidden max-w-2xl mx-auto glass-panel p-14 rounded-[56px] space-y-10 animate-in fade-in text-center">
            <h2 class="text-7xl impact-font tracking-tighter">Loadout</h2>
            <div id="setup-choice" class="grid grid-cols-2 gap-6">
                <button onclick="setMode('custom')" class="p-10 rounded-3xl bg-white/5 border border-white/10 uppercase font-bold tracking-widest transition-all">Manual</button>
                <button onclick="setMode('curriculum')" class="p-10 rounded-3xl bg-white/5 border border-white/10 uppercase font-bold tracking-widest transition-all">Academy</button>
            </div>
            <div id="setup-custom" class="hidden space-y-6">
                <textarea id="words-input" class="w-full h-40 bg-white/5 border border-white/10 rounded-[32px] p-6 outline-none text-white text-lg" placeholder="apple, banana..."></textarea>
                <button onclick="launchMission()" class="w-full py-6 bg-white text-black impact-font rounded-full text-2xl shadow-xl">Engage Mission</button>
            </div>
            <div id="setup-curriculum" class="hidden space-y-6">
                <div class="space-y-3 text-left">
                    <label class="flex items-center gap-5 p-6 bg-white/5 rounded-[24px] cursor-pointer hover:bg-white/10"><input type="checkbox" id="check-y2" checked class="w-6 h-6 rounded accent-sky-500"> Year 2 Training</label>
                    <label class="flex items-center gap-5 p-6 bg-white/5 rounded-[24px] cursor-pointer hover:bg-white/10"><input type="checkbox" id="check-y34" checked class="w-6 h-6 rounded accent-sky-500"> Year 3 & 4 Library</label>
                    <label class="flex items-center gap-5 p-6 bg-white/5 rounded-[24px] cursor-pointer hover:bg-white/10"><input type="checkbox" id="check-y56" checked class="w-6 h-6 rounded accent-sky-500"> Year 5 & 6 Library</label>
                </div>
                <button onclick="launchMission()" class="w-full py-6 bg-sky-500 text-white impact-font rounded-full text-2xl shadow-xl">Engage Mission</button>
            </div>
            <button onclick="showHome()" class="w-full text-white/20 uppercase text-xs font-bold hover:text-white transition">Abort to Hangar</button>
        </div>

        <!-- GAMEPLAY -->
        <div id="game-view" class="hidden glass-panel p-12 rounded-[56px] text-center max-w-4xl mx-auto">
             <div class="flex justify-between mb-8 text-[11px] tracking-widest text-white/40 uppercase font-bold">
                <div id="game-stats">Round 1 / 10</div><div id="game-lives" class="flex gap-4 text-2xl impact-font"></div>
             </div>
             
             <div id="meteor-container" class="hidden h-56 relative bg-black/50 rounded-[40px] mb-12 overflow-hidden border border-white/5">
                <div id="meteor-obj" class="absolute left-1/2 -translate-x-1/2 text-center">
                    <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#f43f5e" stroke-width="2" class="mb-2 mx-auto"><path d="m13 2-2 3M20 4l-2 2M4 20l2-2M12 12l8 8"/></svg>
                    <div id="meteor-hint" class="text-2xl impact-font text-rose-400 bg-black/90 px-6 py-2 rounded-full whitespace-nowrap"></div>
                </div>
             </div>

             <div id="word-display" class="word-container mb-12 min-h-[6rem]"></div>
             
             <button id="voice-btn" onclick="speakCurrentWord()" class="w-24 h-24 rounded-full bg-white/5 border border-white/10 mx-auto mb-12 flex items-center justify-center hover:bg-white/10 transition text-2xl text-white"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 5L6 9H2v6h4l5 4V5z"></path><path d="M15.54 8.46a5 5 0 0 1 0 7.07"></path><path d="M19.07 4.93a10 10 0 0 1 0 14.14"></path></svg></button>
             
             <div id="keyboard" class="grid grid-cols-6 sm:grid-cols-9 gap-3"></div>
             <div id="multi-choice" class="hidden grid grid-cols-1 sm:grid-cols-2 gap-4"></div>
             
             <div id="echo-controls" class="hidden space-y-6">
                <input type="text" id="echo-input" class="w-full p-8 bg-white/5 border border-white/10 rounded-full text-center text-4xl impact-font tracking-widest focus:border-amber-500 outline-none text-white uppercase shadow-inner" placeholder="..." autocomplete="off">
                <button id="echo-submit" onclick="submitEcho()" class="px-12 py-5 bg-amber-500 text-black impact-font rounded-full text-2xl shadow-2xl hover:scale-105 transition-all">Submit Sync</button>
             </div>
             <input type="text" id="typing-input" class="hidden w-full p-8 bg-white/5 border border-white/10 rounded-full text-center text-4xl impact-font tracking-widest focus:border-rose-500 outline-none text-white shadow-inner" placeholder="TYPE NOW">
        </div>

        <!-- PRIZE SCREEN -->
        <div id="screen-prize" class="hidden glass-panel p-20 rounded-[72px] text-center max-w-3xl mx-auto space-y-12 animate-in fade-in duration-800">
            <h2 id="prize-title" class="text-8xl impact-font tracking-tighter text-green-400">Success</h2>
            <div class="bg-white/5 p-10 rounded-[48px] border border-white/10 shadow-2xl">
                <div id="prize-amount" class="text-9xl impact-font text-green-400 mb-2">+0 SC</div>
                <div class="text-[13px] tracking-[0.5em] text-white/40 uppercase font-bold text-center">Cloud Vault Updated</div>
            </div>
            <button onclick="showHome()" class="w-full py-10 bg-white text-black rounded-full impact-font text-4xl shadow-2xl hover:bg-sky-400 transition-all">Return to Command</button>
        </div>

        <!-- WITHDRAW -->
        <div id="screen-withdraw" class="hidden glass-panel p-20 rounded-[72px] text-center max-w-2xl mx-auto space-y-12 animate-in fade-in">
            <h2 class="text-7xl impact-font text-green-400 font-bold uppercase tracking-tighter leading-none text-center">Vault Withdrawal</h2>
            <div class="text-9xl impact-font text-center" id="withdraw-total">0 SC</div>
            <div class="grid grid-cols-1 gap-6">
                <button onclick="generateCertificate('Golden Ticket')" class="p-10 bg-yellow-500 text-black impact-font rounded-[40px] text-4xl shadow-2xl hover:brightness-110 transition-all">Issue Golden Ticket</button>
                <button onclick="generateCertificate('Mastery Certificate')" class="p-10 bg-sky-500 text-white impact-font rounded-[40px] text-4xl shadow-2xl hover:brightness-110 transition-all">Issue Mastery Cert</button>
            </div>
            <button onclick="showHome()" class="text-white/20 uppercase font-bold text-[12px] tracking-[0.7em] hover:text-white transition w-full text-center">Abort Operation</button>
        </div>
    </div>

    <!-- CERTIFICATE -->
    <div id="certificate-print" class="print-only">
        <div id="certificate-view">
            <h1 id="cert-title" style="font-size: 70px; color: #facc15; margin-bottom: 0;">GOLDEN TICKET</h1>
            <p style="font-size: 26px; margin: 35px 0;">This document certifies that Pilot</p>
            <h2 id="cert-pilot-name" style="font-size: 60px; border-bottom: 5px solid #000; display: inline-block; padding: 0 70px; margin-bottom: 10px; text-transform: uppercase;"></h2>
            <p style="font-size: 18px; margin: 10px 0; font-weight: bold; color: #38bdf8;">Training Level: <span id="cert-level"></span></p>
            <p style="font-size: 26px; margin: 25px 0;">has successfully mastered missions at the</p>
            <h3 style="font-size: 45px; margin-bottom: 50px;">GALACTIC ACADEMY</h3>
            <div style="margin: 45px 0; border: 10px dashed #000; padding: 40px; display: inline-block;"><p id="cert-value" style="font-size: 60px; font-weight: bold; margin: 0;">0 SC</p></div>
            <p id="cert-date" style="font-size: 14px; color: #666;"></p>
        </div>
    </div>

    <script>
        const apiKey = ""; 
        const wordsDB = {
            y2: ["beautiful", "because", "climb", "door", "floor", "poor", "find", "kind", "mind", "behind", "child", "children", "wild", "climb", "most", "only", "both", "old", "cold", "gold", "hold", "told", "every", "everybody", "even", "great", "break", "steak", "pretty", "after", "fast", "last", "past", "father", "class", "grass", "pass", "plant", "path", "bath", "hour", "move", "prove", "improve", "sure", "sugar", "eye", "could", "should", "would", "who", "whole", "any", "many", "clothes", "busy", "people", "water", "again", "half", "money", "parents", "christmas"],
            y34: ["accident", "actual", "address", "answer", "appear", "arrive", "believe", "bicycle", "breath", "breathe", "build", "busy", "calendar", "caught", "centre", "century", "certain", "circle", "complete", "consider", "continue", "decide", "describe", "different", "difficult", "disappear", "early", "earth", "eight", "enough", "exercise", "experience", "experiment", "extreme", "famous", "favourite", "february", "forward", "fruit", "grammar", "group", "guard", "guide", "heard", "heart", "heart", "height", "history", "imagine", "increase", "important", "interest", "island", "knowledge", "learn", "length", "library", "material", "medicine", "mention", "minute", "natural", "naughty", "notice", "occasion", "often", "opposite", "ordinary", "particular", "peculiar", "perhaps", "popular", "position", "possess", "possible", "potatoes", "pressure", "probably", "promise", "purpose", "quarter", "question", "recent", "regular", "reign", "remember", "sentence", "separate", "special", "straight", "strange", "strength", "suppose", "surprise", "therefore", "thought", "through", "various", "weight"],
            y56: ["accommodate", "according", "achieve", "aggressive", "amateur", "ancient", "apparent", "appreciate", "attached", "available", "average", "awkward", "bargain", "bruise", "category", "cemetery", "committee", "communicate", "community", "competition", "conscience", "conscious", "controversy", "convenience", "correspond", "criticise", "curiosity", "definite", "desperate", "determined", "develop", "dictionary", "disastrous", "embarrass", "environment", "equip", "equipped", "equipment", "especially", "exaggerate", "excellent", "existence", "explanation", "familiar", "foreign", "forty", "frequently", "government", "guarantee", "harass", "hindrance", "identity", "immediately", "individual", "interfere", "interrupt", "language", "leisure", "lightning", "marvellous", "mischievous", "muscle", "necessary", "neighbour", "nuisance", "occupy", "occur", "opportunity", "parliament", "persuade", "physical", "prejudice", "privilege", "profession", "programme", "pronunciation", "queue", "recognise", "recommend", "relevant", "restaurant", "rhyme", "rhythm", "sacrifice", "secretary", "shoulder", "signature", "sincere", "sincerely", "soldier", "stomach", "sufficient", "suggest", "symbol", "system", "temperature", "thorough", "twelfth", "variety", "vegetable", "vehicle", "yacht"]
        };

        let state = { pilotName: "", bank: 0, activeLevel: "Library", currentGame: null, mode: null, words: [], correctWords: [], currentRound: 0, lives: 3, currentWord: "", guessed: [], meteorTimer: null, choices: [], echoAttempts: 0 };
        const audioCtx = new (window.AudioContext || window.webkitAudioContext)();

        async function selectPilot(name) {
            state.pilotName = name;
            // RULE 3 & CLOUD LOAD
            if (window.galaxyDB.user) {
                const { getDoc } = await import("https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js");
                const docRef = window.getPilotDoc(name);
                const snap = await getDoc(docRef);
                if (snap.exists()) state.bank = snap.data().credits || 0;
                else state.bank = 0;
            }
            showHome();
        }

        function showHome() { 
            hideAll(); 
            const home = document.getElementById('screen-home');
            if(home) home.classList.remove('hidden');
            const bal = document.getElementById('vault-balance');
            if(bal) bal.innerText = state.bank + " SC";
            document.getElementById('display-pilot-name').innerText = state.pilotName;
            const over = document.getElementById('connection-overlay');
            if(over) over.classList.add('hidden');
        }

        function hideAll() {
            ['screen-auth', 'screen-home', 'screen-setup', 'game-view', 'screen-prize', 'screen-withdraw'].forEach(s => {
                const el = document.getElementById(s);
                if (el) el.classList.add('hidden');
            });
            if (state.meteorTimer) clearInterval(state.meteorTimer);
        }

        window.toggleChat = () => {
            const panel = document.getElementById('chatbot-panel');
            panel.classList.toggle('hidden');
            if (!panel.classList.contains('hidden') && document.getElementById('chatbot-messages').children.length === 0) {
                addBotMessage("Greetings, Pilot! I am your Academy Assistant. Ask for tips or Bank of Mum rewards!");
            }
        };

        function addBotMessage(text) {
            const container = document.getElementById('chatbot-messages');
            const div = document.createElement('div');
            div.className = 'message-ai p-3 rounded-2xl text-[10px] leading-relaxed text-white/90 animate-in slide-in-from-bottom-2';
            div.innerText = text;
            container.appendChild(div);
            container.scrollTop = container.scrollHeight;
            speakAI(text);
        }

        function addUserMessage(text) {
            const container = document.getElementById('chatbot-messages');
            const div = document.createElement('div');
            div.className = 'message-user p-3 rounded-2xl text-[10px] text-white/90';
            div.innerText = text;
            container.appendChild(div);
            container.scrollTop = container.scrollHeight;
        }

        async function sendMessage() {
            const input = document.getElementById('chat-input');
            const text = input.value.trim();
            if (!text) return;
            addUserMessage(text); input.value = "";
            document.getElementById('typing-box').classList.remove('hidden');
            try {
                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`, {
                    method: 'POST', headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{ parts: [{ text: text }] }],
                        systemInstruction: { parts: [{ text: `Explain Year 2-6 spelling rules clearly. British tone. Remind Jesse that SC trades for real cash from Bank of Mum.` }] }
                    })
                });
                const result = await response.json();
                document.getElementById('typing-box').classList.add('hidden');
                addBotMessage(result.candidates?.[0]?.content?.parts?.[0]?.text || "Signal lost.");
            } catch (err) { document.getElementById('typing-box').classList.add('hidden'); addBotMessage("AI Offline."); }
        }

        async function speakAI(text) {
            try {
                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${apiKey}`, {
                    method: 'POST', headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{ parts: [{ text: text }] }],
                        generationConfig: { responseModalities: ["AUDIO"], speechConfig: { voiceConfig: { prebuiltVoiceConfig: { voiceName: "Puck" } } } },
                        model: "gemini-2.5-flash-preview-tts"
                    })
                });
                const result = await response.json();
                const audioData = result.candidates[0].content.parts[0].inlineData.data;
                const binary = atob(audioData);
                const array = new Uint8Array(binary.length);
                for (let i = 0; i < binary.length; i++) array[i] = binary.charCodeAt(i);
                new Audio(URL.createObjectURL(new Blob([array], { type: 'audio/wav' }))).play();
            } catch (e) {}
        }

        function speakCurrentWord() {
            const msg = new SpeechSynthesisUtterance(state.currentWord);
            const voices = window.speechSynthesis.getVoices();
            const britishMale = voices.find(v => (v.lang.startsWith('en-GB')) && (v.name.toLowerCase().includes('male'))) || voices.find(v => v.lang.startsWith('en-GB'));
            if (britishMale) msg.voice = britishMale;
            msg.rate = 0.8; window.speechSynthesis.speak(msg);
        }

        function chooseGame(game) { state.currentGame = game; hideAll(); document.getElementById('screen-setup').classList.remove('hidden'); }
        function showSetup() { hideAll(); document.getElementById('screen-setup').classList.remove('hidden'); }

        function setMode(mode) {
            state.mode = mode;
            document.getElementById('setup-choice').classList.add('hidden');
            document.getElementById(`setup-${mode}`).classList.remove('hidden');
        }

        function launchMission() {
            let list = []; let levels = [];
            if (state.mode === 'custom') {
                list = document.getElementById('words-input').value.split(/[\s,]+/).filter(w => w.length > 1);
                state.activeLevel = "Custom";
            } else {
                if (document.getElementById('check-y2').checked) { list = list.concat(wordsDB.y2); levels.push("Y2"); }
                if (document.getElementById('check-y34').checked) { list = list.concat(wordsDB.y34); levels.push("Y3/4"); }
                if (document.getElementById('check-y56').checked) { list = list.concat(wordsDB.y56); levels.push("Y5/6"); }
                state.activeLevel = levels.join(" + ");
                list.sort(() => Math.random() - 0.5); list = list.slice(0, 10);
            }
            if (list.length === 0) return;
            state.words = list; state.correctWords = []; state.currentRound = 0; state.lives = 3;
            hideAll(); document.getElementById('game-view').classList.remove('hidden');
            nextRound();
        }

        function nextRound() {
            if (state.currentRound >= state.words.length || state.lives <= 0) { endGame(); return; }
            state.currentWord = state.words[state.currentRound].toLowerCase();
            state.guessed = []; state.echoAttempts = 0;
            state.currentRound++; updateGameUI(); speakCurrentWord();
        }

        function updateGameUI() {
            document.getElementById('game-stats').innerText = `Round ${state.currentRound} / ${state.words.length}`;
            const livesDiv = document.getElementById('game-lives');
            livesDiv.innerHTML = '';
            for(let i=0; i<3; i++) {
                const sym = document.createElement('span'); sym.innerText = "£";
                sym.className = i < state.lives ? "text-green-400 glow-money" : "text-white/10";
                livesDiv.appendChild(sym);
            }
            ['keyboard', 'multi-choice', 'typing-input', 'meteor-container', 'echo-controls', 'word-display'].forEach(id => {
                const el = document.getElementById(id); if(el) el.classList.add('hidden');
            });
            document.getElementById('voice-btn').classList.remove('hidden');

            if (state.currentGame === 'lunar') {
                document.getElementById('keyboard').classList.remove('hidden');
                document.getElementById('word-display').classList.remove('hidden');
                renderBlanks(); renderKeyboard();
            } else if (state.currentGame === 'meteor') {
                document.getElementById('meteor-container').classList.remove('hidden');
                document.getElementById('typing-input').classList.remove('hidden');
                startMeteor(); 
            } else if (state.currentGame === 'void') {
                document.getElementById('multi-choice').classList.remove('hidden');
                renderMultiChoice(4); 
            } else if (state.currentGame === 'echo') {
                document.getElementById('echo-controls').classList.remove('hidden');
                document.getElementById('echo-input').value = "";
                document.getElementById('echo-input').focus();
            }
        }

        function renderBlanks() {
            const display = document.getElementById('word-display');
            display.innerHTML = '';
            state.currentWord.split('').forEach(char => {
                const s = document.createElement('div');
                s.className = 'blank';
                s.innerText = state.guessed.includes(char) ? char.toUpperCase() : '';
                display.appendChild(s);
            });
        }

        function handleGuess(l) {
            state.guessed.push(l);
            if (!state.currentWord.includes(l)) state.lives--;
            renderBlanks();
            if (state.currentWord.split('').every(c => state.guessed.includes(c))) {
                state.bank += 10; state.correctWords.push(state.currentWord);
                setTimeout(nextRound, 800);
            } else if (state.lives <= 0) setTimeout(nextRound, 800);
            else updateGameUI();
        }

        function submitEcho() {
            const inputEl = document.getElementById('echo-input');
            const val = inputEl.value.trim().toLowerCase();
            if(!val) return;
            if (val === state.currentWord) {
                state.bank += 15; state.correctWords.push(state.currentWord);
                showEchoAnalysis(val); setTimeout(nextRound, 1200);
            } else {
                state.lives--; state.echoAttempts++;
                showEchoAnalysis(val);
                if (state.echoAttempts >= 2 || state.lives <= 0) {
                    setTimeout(nextRound, 2500);
                } else {
                    setTimeout(() => {
                        inputEl.value = ""; inputEl.focus();
                        document.getElementById('word-display').classList.add('hidden');
                    }, 2000);
                }
            }
        }

        function showEchoAnalysis(attempt) {
            const display = document.getElementById('word-display');
            display.innerHTML = ''; display.classList.remove('hidden');
            const target = state.currentWord.split('');
            const typed = attempt.split('');
            typed.forEach((char, i) => {
                const s = document.createElement('div');
                s.className = 'blank'; s.innerText = char.toUpperCase();
                if (char === target[i]) s.classList.add('correct-char');
                else s.classList.add('error-char');
                display.appendChild(s);
            });
            if (typed.length < target.length) {
                for (let i = typed.length; i < target.length; i++) {
                    const s = document.createElement('div');
                    s.className = 'blank error-char'; s.innerText = '?';
                    display.appendChild(s);
                }
            }
        }

        function renderKeyboard() {
            const kb = document.getElementById('keyboard'); kb.innerHTML = '';
            'abcdefghijklmnopqrstuvwxyz'.split('').forEach(l => {
                const b = document.createElement('button'); b.innerText = l.toUpperCase();
                b.className = `p-4 rounded-2xl font-bold transition ${state.guessed.includes(l) ? 'opacity-20' : 'bg-white/5 border border-white/10 hover:bg-white/10'}`;
                b.disabled = state.guessed.includes(l);
                b.onclick = () => handleGuess(l);
                kb.appendChild(b);
            });
        }

        function startMeteor() {
            const input = document.getElementById('typing-input');
            const obj = document.getElementById('meteor-obj');
            document.getElementById('meteor-hint').innerText = state.currentWord;
            input.value = ''; input.focus();
            let pos = 0; clearInterval(state.meteorTimer);
            state.meteorTimer = setInterval(() => {
                pos += 1.8; obj.style.top = pos + "px";
                if (pos > 200) { clearInterval(state.meteorTimer); state.lives--; nextRound(); }
            }, 50);
            input.oninput = () => {
                if (input.value.toLowerCase() === state.currentWord) {
                    clearInterval(state.meteorTimer); state.bank += 10; state.correctWords.push(state.currentWord);
                    setTimeout(nextRound, 500);
                }
            };
        }

        function renderMultiChoice(count) {
            const container = document.getElementById('multi-choice'); container.innerHTML = '';
            const options = [state.currentWord];
            while(options.length < count) {
                const typo = makeBelievableTypo(state.currentWord);
                if (!options.includes(typo)) options.push(typo);
            }
            options.sort(() => Math.random() - 0.5);
            options.forEach((opt, idx) => {
                const b = document.createElement('button');
                b.innerHTML = `<span class="text-sky-400 mr-4">${idx+1}.</span> ${opt.toUpperCase()}`;
                b.className = 'p-10 rounded-3xl bg-white/5 border border-white/10 hover:border-sky-400 impact-font text-2xl transition text-left shadow-lg';
                b.onclick = () => handleChoiceSelection(opt);
                container.appendChild(b);
            });
        }

        function handleChoiceSelection(opt) {
            if (opt === state.currentWord) { 
                state.bank += 10; state.correctWords.push(state.currentWord);
                state.currentWord.split('').forEach(c => state.guessed.push(c));
                document.getElementById('word-display').classList.remove('hidden');
                renderBlanks(); setTimeout(nextRound, 800); 
            } else { state.lives--; nextRound(); }
        }

        function makeBelievableTypo(w) {
            let word = w.toLowerCase();
            const rules = [
                (s) => s.replace(/(.)\1/g, "$1"),
                (s) => s.replace(/ce$/, "se").replace(/se$/, "ce").replace(/able$/, "ible").replace(/ible$/, "able"),
                (s) => s.replace(/ch/g, "c").replace(/kn/g, "n").replace(/gh/g, "g"),
                (s) => s.replace(/ie/g, "ei").replace(/ei/g, "ie"),
                (s) => s.replace(/ary$/, "ery").replace(/ery$/, "ary"),
                (s) => s.replace(/tion$/, "sion").replace(/sion$/, "tion")
            ];
            for (let i = 0; i < 5; i++) {
                let strategy = rules[Math.floor(Math.random() * rules.length)];
                let typo = strategy(word);
                if (typo !== word) return typo;
            }
            return word.substring(0, word.length-1) + (word.endsWith('e') ? 'i' : 'e'); 
        }

        async function endGame() {
            clearInterval(state.meteorTimer);
            hideAll();
            document.getElementById('screen-prize').classList.remove('hidden');
            document.getElementById('prize-amount').innerText = "+" + (state.correctWords.length * 10) + " SC";
            
            // RULE 1 & 3: Save to Cloud
            if (window.galaxyDB.user) {
                const { setDoc } = await import("https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js");
                const docRef = window.getPilotDoc(state.pilotName);
                await setDoc(docRef, { credits: state.bank, lastLevel: state.activeLevel }, { merge: true });
            }
        }

        function showWithdraw() { hideAll(); document.getElementById('screen-withdraw').classList.remove('hidden'); document.getElementById('withdraw-total').innerText = state.bank + " SC"; }

        function generateCertificate(type) {
            document.getElementById('cert-pilot-name').innerText = state.pilotName;
            document.getElementById('cert-title').innerText = type.toUpperCase();
            document.getElementById('cert-value').innerText = state.bank + " SC";
            document.getElementById('cert-level').innerText = state.activeLevel;
            document.getElementById('cert-date').innerText = new Date().toLocaleDateString();
            window.print();
        }

        window.addEventListener('keydown', (e) => {
            if(e.key === 'Enter') {
                if(state.currentGame === 'echo' && !document.getElementById('echo-controls').classList.contains('hidden')) submitEcho();
            }
        });

        window.onload = () => { hideAll(); document.getElementById('screen-auth').classList.remove('hidden'); };
        window.speechSynthesis.onvoiceschanged = () => window.speechSynthesis.getVoices();
    </script>
</body>
</html>